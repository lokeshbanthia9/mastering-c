/**
 * Program to check if the expression `getchar() != EOF` is `0` or `1`.
 * 
 * Exercise 1.6
 */

#include <stdio.h>

int main()
{
  printf("getchar() != EOF: %d\n", getchar() != EOF);
  return 0;
}