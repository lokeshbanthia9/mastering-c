#include <stdio.h>

// This is a comment /* same comment */ still same /*
int main() {
    printf("This is not a comment: /* But it looks like one */\n");
    printf("Another one: // This too\n");
    return 0;
}
